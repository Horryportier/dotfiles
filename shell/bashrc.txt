if [[ -f ~/.config/nnn/nnn_conf.sh ]]; then
    . ~/.config/nnn/nnn_conf.sh
fi

export EDITOR='nvim'

source /home/horryportier/.fzf-complete/fzf-bash-completion.sh
bind -x '"\t": fzf_bash_completion'

eval "$(starship init bash)"

. "$HOME/.cargo/env"


echo -e "$(neofetch --source ~/hell_paradise_logo.txt --colors 13 13 14 14 13 --ascii_colors 13  --color_blocks off )"

[ -f ~/.fzf.bash ] && source ~/.fzf.bash
